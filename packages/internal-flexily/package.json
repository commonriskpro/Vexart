{
  "name": "flexily",
  "version": "0.6.0",
  "description": "Pure JavaScript flexbox layout engine — vendored internal workspace",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./types/index.d.ts",
  "exports": {
    ".": {
      "types": "./types/index.d.ts",
      "import": "./src/index.ts"
    }
  },
  "license": "MIT",
  "scripts": {
    "build": "bun run build:js && bun run build:types",
    "build:js": "bun build ./src/index.ts --outdir dist --target bun --format esm --external debug --external loggily",
    "build:types": "tsc -p ./tsconfig.json",
    "typecheck": "tsc -p ./tsconfig.json --noEmit"
  },
  "dependencies": {
    "debug": "4.4.0",
    "loggily": "0.4.2"
  },
  "devDependencies": {
    "@types/bun": "1.3.12",
    "typescript": "^5.7.0",
    "@types/debug": "4.1.13"
  }
}
