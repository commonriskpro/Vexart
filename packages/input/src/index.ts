/**
 * @tge/input — Parse raw terminal bytes into semantic events.
 *
 * Transforms stdin bytes into typed events:
 * - KeyEvent (key, modifiers, repeat)
 * - MouseEvent (click, move, scroll, drag) via SGR protocol
 * - FocusEvent (focus in/out)
 * - Kitty keyboard protocol for enhanced key detection
 */

// TODO: Phase 1 — implement keyboard parser, mouse protocol
export {};
