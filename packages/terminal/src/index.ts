/**
 * @tge/terminal — Terminal detection, capabilities, lifecycle, and raw I/O.
 *
 * This is the lowest layer. It talks directly to the terminal:
 * - Detects which terminal (Ghostty, Kitty, iTerm, xterm, etc.)
 * - Queries capabilities (Kitty graphics, 24-bit color, mouse protocol)
 * - Manages lifecycle (alternate screen, raw mode, resize, cleanup)
 * - Provides raw stdin/stdout access
 */

// TODO: Phase 1 — implement detect, caps, stdin, stdout, lifecycle
export {};
