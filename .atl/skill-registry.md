# Vexart — Skill Registry

Auto-generated by `sdd-init`. Lists user-level skills available to AI agents working on this project, filtered by relevance to the Vexart stack.

## Project conventions

- `/Users/dev/vexart/AGENTS.md` — primary project instructions (personality, style guide, module reference, interaction patterns).
- `/Users/dev/vexart/docs/PRD.md` — product requirements (what and why).
- `/Users/dev/vexart/docs/ARCHITECTURE.md` — target architecture (how).
- `/Users/dev/vexart/docs/API-POLICY.md` — public API rules.
- `/Users/dev/vexart/openspec/config.yaml` — SDD rules per phase.
- `/Users/saturno/.config/opencode/AGENTS.md` — user-level global conventions.

## Relevant skills (triggered by Vexart context)

### TypeScript / reactivity

| Skill | When it applies to Vexart |
| ----- | ------------------------- |
| `typescript` | Any TS code — strict types, generics, interfaces. Core language for engine/primitives/headless/styled. |
| `zod-4` | Config/schema validation (e.g. `mount()` options, theme descriptor). |

### SDD workflow (always on during change execution)

| Skill | When it applies |
| ----- | --------------- |
| `sdd-init` | Already run — this file is the output. |
| `sdd-explore` | Before committing to a change (e.g. investigating Taffy/WGPU versions for Phase 2). |
| `sdd-propose` | Creating a new change proposal (every phase starts here). |
| `sdd-spec` | Writing requirements for a change. |
| `sdd-design` | Technical design for a change. |
| `sdd-tasks` | Breaking design into atomic tasks. |
| `sdd-apply` | Implementing one or more tasks. |
| `sdd-verify` | Validating implementation against specs. |
| `sdd-archive` | Merging delta specs to main specs after a phase completes. |

### Testing

| Skill | When it applies |
| ----- | --------------- |
| *(none active yet)* | No JS test skill required — `bun test` is lightweight. |

**Future**: when Rust tests grow past smoke level, add a `rust-testing` skill. When golden-image suite lands (Phase 4), add a visual-testing skill.

### Review / communication

| Skill | When it applies |
| ----- | --------------- |
| `maintainer-voice` | Writing PR descriptions, changelog entries, release notes. |
| `judgment-day` | Adversarial review of a phase before marking it done. |
| `pr-review` | Reviewing external contributions (post v0.9 public launch). |
| `release-note-safety` | Composing release notes (v0.9 launch). |

### NOT applicable to Vexart

These user-level skills are explicitly **not** relevant — do not load them:

- `nextjs-15`, `react-19`, `tailwind-4` — Vexart is terminal-native, not web.
- `angular`, `django-drf`, `dotnet`, `pytest` — wrong stack.
- `gentleman-*` — different project (Gentleman.Dots installer).
- `ai-sdk-5`, `zustand-5` — no AI chat, no Zustand (Vexart uses SolidJS signals).
- `go-testing` — no Go in Vexart.
- `homebrew-release`, `jira-*`, `stream-deck`, `backlog-triage`, `repo-hardening`, `branch-pr`, `issue-creation` — out of scope until post-v0.9 GTM.

## Policy

- **Always load** skills triggered by explicit context (`skill` tool invocation).
- **Never load** skills from the "NOT applicable" section without founder approval.
- Re-run `sdd-init` or `/update-skills` when new skills are added globally.
