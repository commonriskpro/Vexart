[toolchain]
channel = "1.95.0"
components = ["rustfmt", "clippy", "rust-src"]
targets = ["aarch64-apple-darwin", "x86_64-unknown-linux-gnu", "aarch64-unknown-linux-gnu"]
# darwin-x64 (macOS Intel) omitted — macos-13 runners are scarce on GitHub free tier
profile = "minimal"
