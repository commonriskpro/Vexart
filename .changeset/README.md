# Changesets

Welcome! This folder holds pending changeset files for Vexart.

## Adding a changeset

When making changes that affect users, run:

```bash
bun run changeset
```

Follow the prompts to select the packages changed (typically `vexart`), specify the change type (`patch`, `minor`, `major`), and write a clear, user-facing summary of the change.
