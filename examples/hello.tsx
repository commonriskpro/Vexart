/**
 * TGE Hello World — Phase 2 target.
 *
 * This is what we're building toward. When Phase 2 is done,
 * this code should render a pixel-perfect rounded card with
 * shadow and text in the terminal.
 *
 * Phase 1 target: the same visual but via imperative API.
 */

/*
import { render } from "@tge/renderer"
import { Box, Text } from "@tge/components"

function App() {
  return (
    <Box
      padding={16}
      backgroundColor="#1a1a2e"
      width="100%"
      height="100%"
      alignItems="center"
      justifyContent="center"
    >
      <Box
        backgroundColor="#16213e"
        cornerRadius={12}
        padding={24}
        shadow={{ blur: 16, color: "#00000088" }}
      >
        <Text color="#e0e0e0" fontSize={16}>
          Hello from TGE
        </Text>
        <Text color="#0f3460" fontSize={12}>
          Pixel-native terminal rendering
        </Text>
      </Box>
    </Box>
  )
}

render(() => <App />)
*/

console.log("TGE example — JSX rendering not yet implemented.");
console.log("Phase 1 target: render a rounded rect via imperative API.");
console.log("Phase 2 target: render the JSX above.");
