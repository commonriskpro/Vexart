node_modules/
zig-cache/
zig-out/
.zig-cache/
*.dylib
*.so
*.dll
dist/
.DS_Store
bun.lockb
dist.bak/
docs.md
zig/libtge.a

# Cargo workspace build artifacts — regenerated by `cargo build`.
# Cargo.lock IS tracked (we build a cdylib application, not a published
# library, so reproducible builds matter). See docs/PRD.md §10.1 / DEC-003.
target/
.api-extractor-temp/
packages/*/etc/.temp/
scripts/frame-breakdown-report.json
scripts/terminal-transport-report.json
package/
